# The code works.
# Changes to be made.
# 1. Ensure no latency (now little latency is there while recognizing.)
# 2. Order of the functionality must be changed.
  -> First capture the  image while registration phase.
  -> then create the database and push the data(imgs) and then use the database.
  -> Recognize using the .pkl file.
# Ensure Multi-threaded way (for recoginze) for smooth transition. 
